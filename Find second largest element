int print2largest(int arr[], int arr_size)
    {
    	int max=-1, s=-1;
    	for(int i=0;i<arr_size;i++){
    	    if(arr[i]>max){
    	        s=max;
    	        max=arr[i];
    	    }
    	    else if(arr[i]<max && arr[i]>s){
    	        s=arr[i];
    	    }
    	}
    	return s;
    
    }
